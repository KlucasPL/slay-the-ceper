/**
 * Fixed tutorial content to prevent accidental drift during refactors.
 */

/** @type {string} */
export const tutorialFixedRelicId = 'bilet_tpn';

/** @type {string[]} */
export const tutorialFixedCardRewardIds = ['ciupaga', 'gasior', 'hej'];

/** @type {Array<'fight' | 'event' | 'shop' | 'campfire' | 'elite'>} */
export const tutorialMapSequence = ['fight', 'event', 'shop', 'campfire', 'elite'];
